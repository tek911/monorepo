# Auth Service Configuration
# WARNING: Contains intentionally vulnerable configurations

server.port=8080
spring.application.name=auth-service

# Database Configuration
# VULNERABILITY: Hardcoded credentials
spring.datasource.url=jdbc:mysql://localhost:3306/authdb
spring.datasource.username=root
spring.datasource.password=P@ssw0rd123!

# H2 Console (for development)
# VULNERABILITY: H2 console enabled in production config
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# VULNERABILITY: SQL logging with parameters
spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration
# VULNERABILITY: Debug logging in production
logging.level.root=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
# VULNERABILITY: JWT secret in properties file
jwt.secret=mySuper$ecretKey123!@#
jwt.expiration=31536000000

# Session Configuration
# VULNERABILITY: Long session timeout
server.servlet.session.timeout=24h
# VULNERABILITY: Session cookie without secure flag
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.http-only=false

# File Upload
# VULNERABILITY: Large file upload allowed
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Error Handling
# VULNERABILITY: Detailed errors exposed
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
server.error.include-exception=true

# Actuator (if enabled)
# VULNERABILITY: All actuator endpoints exposed
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# AWS Configuration (Fake but valid format)
# VULNERABILITY: Hardcoded AWS credentials
aws.access.key=AKIAIOSFODNN7EXAMPLE
aws.secret.key=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
aws.region=us-east-1

# Email Configuration
# VULNERABILITY: SMTP credentials in config
spring.mail.host=smtp.gmail.com
spring.mail.username=noreply@vulnmonolith.com
spring.mail.password=EmailP@ss123!

# Redis Configuration
# VULNERABILITY: Redis password in config
spring.redis.host=localhost
spring.redis.password=RedisP@ssword!

# Encryption Keys
# VULNERABILITY: Encryption key in properties
encryption.key=aes-256-key-1234567890123456
encryption.iv=1234567890123456
